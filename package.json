{
  "name": "astro-theory",
  "description": "Celestrium — Astrophysical Intelligence Engine & Research Platform",
  "private": true,
  "scripts": {
    "build": "npm --prefix site run build",
    "deploy": "npm --prefix site run build && npx wrangler deploy"
  }
}

{
  "name": "astro-theory",
  "description": "Celestrium — Astrophysical Intelligence Engine & Research Platform",
  "private": true,
  "workspaces": [
    "site"
  ],
  "scripts": {
    "build": "npm run build --workspace=site",
    "deploy": "npm run build --workspace=site && npx wrangler deploy"
  }
}

{
  "name": "astro-theory",
  "description": "Celestrium — Astrophysical Intelligence Engine & Research Platform",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix site",
    "deploy": "npm run build --prefix site && npx wrangler deploy"
  }
}

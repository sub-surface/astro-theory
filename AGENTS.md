# AGENT.md

Celestrium's agent onboarding lives in **[`CLAUDE.md`](./CLAUDE.md)** — read it first.

It covers what Celestrium is (a three-wing astrophysics instrument: theory workshop ·
experimental validation · imaging & recreation), the repo layout (`celestrium/` engine,
`docs/` + `docs/research/`), the architecture rule (logic in `core/` and `caps/`; `cli.py`
presents), the active line (G-Euclid DR1 prep), the working conventions, and the note that
a second agent (Codex) also commits here.

(Parent hub map: [`../AGENTS.md`](../AGENTS.md).)


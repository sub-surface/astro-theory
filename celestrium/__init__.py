"""celestrium/ — a three-wing astrophysics instrument.

Theory workshop (literature → ideas), experimental validation (data slices →
empirical tests), imaging & recreation (diagrams + renders). Query snippets
(gaia, euclid, irsa, mast, heasarc, vizier, datalab_desi, vo_generic), plus
cache (provenance), xmatch (audits), resolvers (object ID), ads (literature),
cutouts (imaging). On top: a shared service layer — registry (archives/recipes/
targets/runbooks as data) + packets (dataclass builders) — that the hub CLI and
future TUI both present over. See README.md and ../docs/roadmap.md.
Run it: `python -m celestrium --help`.
"""
